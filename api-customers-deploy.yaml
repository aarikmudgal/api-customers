apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: api-customers
spec:
  minReadySeconds: 5
  replicas: 1
  template:
    metadata:
      labels:
        name: api-customers
    spec:
      containers:
      - name: api-customers
        image: gcr.io/ancient-yeti-166518/api-customers:latest
        ports:
        - containerPort: 8001
